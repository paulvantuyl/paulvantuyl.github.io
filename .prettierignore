# Legacy Jekyll site (contains Liquid tags Prettier can't parse)
_archive

# Auto-generated blog pagination data (regenerated by `npm run generate:posts`)
public/weblog

# Historical blog post content - not reformatting old prose
src/content/posts

# Build/dependency artifacts (also covered by .gitignore, listed explicitly
# so this file doesn't silently depend on staying in sync with it)
dist
storybook-static
node_modules
coverage
build
package-lock.json
